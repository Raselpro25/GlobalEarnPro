<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Leaderboard - GlobalEarnPro</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #1f1c2c, #928dab);
      color: #fff;
      padding: 20px;
    }

    h1 {
      text-align: center;
      font-size: 28px;
      color: #ff69b4;
      margin-bottom: 30px;
      text-shadow: 0 0 10px #ff69b4;
    }

    .trophy {
      text-align: center;
      font-size: 40px;
      margin-bottom: 10px;
      color: gold;
      text-shadow: 0 0 15px gold;
    }

    .leaderboard {
      max-width: 700px;
      margin: auto;
    }

    .user-card {
      background: rgba(0, 0, 0, 0.2);
      border-radius: 10px;
      padding: 15px;
      margin: 10px 0;
      border-left: 5px solid #00ffcc;
      box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
      transition: transform 0.2s;
    }

    .user-card:hover {
      transform: scale(1.02);
      box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
    }

    .user-info {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .user-rank {
      font-size: 20px;
      font-weight: bold;
      color: #00ffcc;
    }

    .user-name {
      font-size: 18px;
      color: #fff;
    }

    .user-coins {
      font-size: 16px;
      color: #7fffd4;
    }

    .back {
      text-align: center;
      margin-top: 30px;
    }

    .back a {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
    }

    footer {
      text-align: center;
      margin-top: 40px;
      color: #ccc;
      font-size: 14px;
    }

    footer a {
      color: #fff;
      text-decoration: none;
      margin: 0 10px;
    }

    footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 500px) {
      .user-info {
        flex-direction: column;
        align-items: flex-start;
      }
      .user-coins {
        margin-top: 5px;
      }
    }
  </style>
</head>
<body>
  <div class="leaderboard">
    <div class="trophy">🏆</div>
    <h1>Top Earners</h1>

    <% const sortedUsers = [...(allUsers || [])].sort((a, b) => b.coins - a.coins).slice(0, 10); %>
    <% sortedUsers.forEach((user, index) => { %>
      <div class="user-card">
        <div class="user-info">
          <span class="user-rank">#<%= index + 1 %></span>
          <span class="user-name"><%= user.username %></span>
          <span class="user-coins"><%= user.coins %> coins</span>
        </div>
      </div>
    <% }) %>

    <% if (sortedUsers.length === 0) { %>
      <p style="text-align:center;">No users found.</p>
    <% } %>

    <div class="back">
      <a href="/dashboard?email=<%= user.email %>">← Back to Dashboard</a>
    </div>
  </div>

  <footer>
    <a href="/contact">Contact</a> |
    <a href="/terms">Terms</a> |
    <a href="/privacy">Privacy</a>
  </footer>
</body>
</html>
